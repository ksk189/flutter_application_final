import 'package:flutter/material.dart';

class AgriTech extends StatelessWidget {
  const AgriTech({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Agri tech is under construction'),
      ),
    );
  }
}
