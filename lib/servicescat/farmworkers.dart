import 'package:flutter/material.dart';

class FarmWorkersPage extends StatelessWidget {
  const FarmWorkersPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Farm workersis under construction'),
      ),
    );
  }
}
